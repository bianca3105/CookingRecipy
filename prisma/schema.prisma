generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model Folder {
  id      String   @id @default(cuid())
  key     String   @unique
  name    String
  order   Int      @default(0)
  recipes Recipe[]
}

model Recipe {
  id          String      @id @default(cuid())
  name        String
  ingredients Json
  steps       Json
  servings    String?
  totalTime   String?
  sourceUrl   String?
  imageUrl    String?
  folderId    String
  folder      Folder      @relation(fields: [folderId], references: [id])
  menuEntries MenuEntry[]
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt

  @@index([folderId])
}

model Week {
  id          String      @id @default(cuid())
  startDate   DateTime    @unique
  menuEntries MenuEntry[]
}

model MenuEntry {
  id       String   @id @default(cuid())
  weekId   String
  week     Week     @relation(fields: [weekId], references: [id], onDelete: Cascade)
  dayIndex Int
  mealType String
  recipeId String
  recipe   Recipe   @relation(fields: [recipeId], references: [id], onDelete: Cascade)

  @@unique([weekId, dayIndex, mealType, recipeId])
  @@index([weekId, dayIndex, mealType])
  @@index([recipeId])
}
